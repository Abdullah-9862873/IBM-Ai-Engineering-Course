# Quiz: Deep Networks

## Questions

### Question 1
What is the primary advantage of adding more hidden layers to a neural network?

- It increases the performance of the model while reducing overfitting
- It reduces the need for data normalization
- It decreases the model's computational complexity
- It always prevents overfitting.

### Question 2
In the context of deep neural networks, what does the parameter H1 represent?

- The number of neurons in the first hidden layer
- The number of layers in the network
- The number of neurons in the output layer
- The size of the input dimensions

### Question 3
What is the primary purpose of the dropout method in neural networks?

- To increase the learning rate
- To reduce overfitting by randomly turning off neurons during training
- To decrease the model's training time
- To ensure all neurons are always active

### Question 4
What is the typical range of the probability "p" used in the dropout method?

- 0.5 to 0.9
- 0.9 to 1.0
- 0.0 to 0.5
- 0.0 to 0.1

### Question 5
Why is it important to randomly initialize the weights in a neural network?

- To ensure all neurons start with the same values
- To reduce the need for activation functions
- To prevent all neurons in the same layer from having the same output
- To increase the training speed

### Question 6
Which initialization method is typically used with the ReLU activation function to mitigate the vanishing gradient problem?

- Zero initialization
- Xavier initialization
- Uniform initialization
- He initialization

### Question 7
What is the primary benefit of using momentum in gradient descent?

- It reduces the learning rate
- It increases the model's accuracy
- It accelerates convergence and helps avoid local minima
- It prevents overfitting

### Question 8
In the analogy used in the video, what does the momentum term (ρ) in gradient descent resemble?

- The mass of a ball
- The acceleration of a car
- The learning rate of the algorithm
- The slope of the cost function

### Question 9
What is the main purpose of batch normalization in a neural network?

- To decrease the size of the network
- To normalize the output of each layer to improve training stability
- To eliminate the need for activation functions
- To increase the learning rate

### Question 10
What are the parameters learned during batch normalization?

- Mean and variance of the data
- Learning rate and momentum
- Weight decay and dropout rate
- Scaling and shifting parameters for normalization

---

## Answers

1. It increases the performance of the model while reducing overfitting
2. The number of neurons in the first hidden layer
3. To reduce overfitting by randomly turning off neurons during training
4. 0.0 to 0.5
5. To prevent all neurons in the same layer from having the same output
6. He initialization
7. It accelerates convergence and helps avoid local minima
8. The mass of a ball
9. To normalize the output of each layer to improve training stability
10. Scaling and shifting parameters for normalization