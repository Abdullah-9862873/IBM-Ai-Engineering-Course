# Quiz: Fine-Tuning Causal LLMs with Human Feedback and Direct Preference

## Question 1
What is the core principle behind instruction-tuning in LLMs?

- **Applying supervised fine-tuning on task-specific instruction-output datasets** ✓
- Leveraging pretraining on next token prediction for datasets
- Improving weight using masked language modeling in datasets
- Enforcing reinforcement learning from real-time user inputs

---

## Question 2
A machine learning engineer wants to validate an instruction-tuned model's response generation. Which function should they use to format input prompts during validation?

- **formatting_prompts_func_no_response** ✓
- format_validation_output
- create_prompt_template
- format_for_finetuning

---

## Question 3
You are training a chatbot to prefer responses supporting cats. It chooses a response that enthusiastically endorses cats. How does reward modeling improve a chatbot's response selection?

- By accelerating the model's execution capabilities
- By reducing the training parameters count for the model
- **By tuning the model's responses based on users' interests** ✓
- By expanding the training model's vocabulary capacity

---

## Question 4
In the context of the Bradley-Terry model for reward model loss in machine learning, which of the following is correct?

- The Bradley-Terry model assigns probabilities by taking the ratio of the score of one item to the sum of scores in a pairwise comparison.
- The Bradley-Terry model assumes that the probability of one response being preferred over another is proportional to the difference in their scores.
- **The Bradley-Terry model computes the probability of one item over another using a sigmoid function.** ✓
- The Bradley-Terry model assigns a higher probability to the item with the higher score without considering the score of the other item.

---

## Question 5
Thomas wants to start training as a reward model after setting up RewardTrainer. Which method should he use?

- reward_config.start()
- **trainer.train()** ✓
- trainer.loop()
- train_model_now()

---

## Question 6
Vicky, a machine learning expert, wants to build an instruction-following LLM for a QA system. Why does he apply instruction-tuning before reinforcement learning in the LLM training process?

- **To provide the model with structured instruction-response pairs** ✓
- To pretrain the model on unstructured web content
- To evaluate the model based on unseen questions
- To train the model preferences through ranking samples

---

## Question 7
You want to apply a reward modeling function to the dataset. Which of the following functions will help you organize prompts and responses?

- compute_rewards
- validate_outputs
- **get_response** ✓
- preprocess_tokens

---

## Question 8
In the SFTConfig, what does max_seq_length control?

- The batch sizes for training and evaluation
- **The maximum length of the output responses** ✓
- The number of training epochs
- The evaluation time

---

## Question 9
Which method is useful to evaluate responses in reward modeling for generative AI?

- Compare model responses with a pre-defined set of correct answers (references).
- **Allow human evaluators to rank responses based on quality.** ✓
- Analyze the diversity of responses generated by a model.
- Measure differences between the predicted rewards and actual rewards received.

---

## Question 10
According to the Bradley-Terry Model, what is true for the sigmoid function?

- **Interprets the differences between good and bad responses** ✓
- Finds the parameters for good and bad responses
- Minimizes the differences between good and bad responses
- Measures reward quality for the good and bad responses

---

## Question 11
In large language models (LLMs), which of the following is true for the next token 't+1' distribution based on the current token 't'?

- Uniform distribution of token 't+1' with a unique probability of getting selected
- **Condition token 't+1' on the current token 't' and the entire preceding sequence of tokens** ✓
- Random token 't+1' selection from the possible tokens, independent of 't'
- Determine token 't+1' solely with respect to the specific token 't', without considering any prior context

---

## Question 12
Regarding the language models, how does a policy relate to the model's distribution for the possible outputs?

- A policy is a deterministic set of output.
- A policy is the same as the probability distribution, representing all possible output with the same probability.
- A policy distribution represents all possible outputs based on the predefined rules.
- **A policy for distributing tokens conditioned on a given set of query tokens.** ✓

---

## Question 13
In reinforcement learning with human feedback (RLHF), which of the following is true to guide the model's learning process using the concept of expected rewards?

- The model selects outputs to maximize the sum of possible rewards for each token.
- The model randomly selects actions by considering expected rewards or human feedback
- The model ignores human feedback and relies on a predefined reward function to determine the expected reward.
- **The model selects outputs that maximize the expected reward and prioritizes actions so that the model receives positive human feedback.** ✓

---

## Question 14
In the Venn diagram below, what is the role of the KL penalty coefficient in the policy gradient methods?

- Apply trust region constraint to limit the policy from deviating too far from the previous policy
- **Regulating the divergence between the old and the new policies** ✓
- Adjusting the learning rate during training, ensuring that the policy update steps are appropriately sized
- Restricting the policy updates to a small region around the old policy

---

## Question 15
What would happen if you set the sentiment score to 0 in the given code snippet for the PPO trainer?

- Setting the sentiment score to 0 loops the code for the PPO algorithm.
- **Setting the sentiment score to 0 increases the chances of generating higher rewards for negative sentiment.** ✓
- Setting the sentiment score to 0 increases the PPO mean reward over time.
- Setting the sentiment score to 0 rewards positive sentiment more.

---

## Question 16
Which of the following statements correctly describes the computation of the expected rewards?

- The expected reward is defined as the reward obtained from all the possible actions in the given state, regardless of the action taken.
- **Expected reward is the weighted sum of the future rewards, predicted by the reward model, given the current state and action.** ✓
- Expected reward is defined as the sum of the immediate rewards for the particular action, averaged over all possible states.
- The expected reward is obtained from the best action evaluated by human feedback without considering future rewards.

---

# Answers

1. **Applying supervised fine-tuning on task-specific instruction-output datasets** - Instruction-tuning trains models on instruction-output pairs using supervised fine-tuning.

2. **formatting_prompts_func_no_response** - Used for validation without response labels.

3. **By tuning the model's responses based on users' interests** - Reward modeling adjusts responses based on user preferences.

4. **The Bradley-Terry model computes the probability of one item over another using a sigmoid function** - Uses sigmoid to convert reward difference to probability.

5. **trainer.train()** - The RewardTrainer uses trainer.train() to begin training.

6. **To provide the model with structured instruction-response pairs** - Instruction-tuning establishes foundational understanding before RLHF.

7. **get_response** - Organizes prompts and responses for reward modeling.

8. **The maximum length of the output responses** - max_seq_length controls maximum sequence length during training.

9. **Allow human evaluators to rank responses based on quality** - Human evaluators ranking responses is most useful for evaluating generative AI responses.

10. **Interprets the differences between good and bad responses** - Sigmoid function interprets differences between responses in Bradley-Terry model.

11. **Condition token 't+1' on the current token 't' and the entire preceding sequence of tokens** - LLMs use causal masking where each token is conditioned on all previous tokens.

12. **A policy for distributing tokens conditioned on a given set of query tokens** - Policy represents probability distribution over possible outputs given input.

13. **The model selects outputs that maximize the expected reward and prioritizes actions so that the model receives positive human feedback** - RLHF guides learning using expected rewards with human feedback.

14. **Regulating the divergence between the old and the new policies** - KL penalty ensures stable training by controlling policy changes.

15. **Setting the sentiment score to 0 increases the chances of generating higher rewards for negative sentiment** - score_change=0 makes the model prefer negative sentiment.

16. **Expected reward is the weighted sum of the future rewards, predicted by the reward model, given the current state and action** - Expected reward considers future rewards, not just immediate ones.

---

## DPO Questions

## Question 17
Which of the following is true about Direct Preference Optimization (DPO)?

- DPO uses a reward model and PPO for optimization
- **DPO directly optimizes model parameters based on human preferences without reward model** ✓
- DPO requires computing the partition function explicitly
- DPO is more computationally expensive than traditional RLHF

---

## Question 18
In DPO, what is the role of the partition function Z(x)?

- It normalizes the probability distribution to sum to 1
- **It is eliminated from the final loss function through clever mathematical manipulation** ✓
- It is computed explicitly for each input
- It represents the reward model in DPO

---

## Question 19
What does the DPO loss function depend on?

- Only the reward model
- **The LLM and its reference model (no separate reward model needed)** ✓
- The partition function
- The PPO algorithm

---

## Question 20
In the DPO objective, what does the Beta parameter control?

- The learning rate
- **The temperature/temperature-like parameter controlling reference model weight** ✓
- The batch size
- The number of training epochs

---

# Answers (DPO)

17. **DPO directly optimizes model parameters based on human preferences without reward model** - DPO reformulates the problem using closed-form optimal policy.

18. **It is eliminated from the final loss function through clever mathematical manipulation** - The partition function cancels out when substituting the optimal policy expression.

19. **The LLM and its reference model (no separate reward model needed)** - DPO loss is function of π_θ and π_ref only.

20. **The temperature/temperature-like parameter controlling reference model weight** - Beta typically ranges from 0.1-0.5 in DPO.

---

## InstructLab Questions

## Question 21
What is InstructLab primarily used for?

- Training models from scratch on large datasets
- **Fine-tuning LLMs locally using synthetic data generation** ✓
- Evaluating model performance on benchmarks
- Converting models between different formats

## Question 22
In InstructLab, what is the role of the teacher model?

- Directly fine-tunes the student model
- **Generates synthetic question-answer pairs from seed examples** ✓
- Evaluates the fine-tuned model
- Serves the model for chat interactions

## Question 23
What is the minimum number of seed examples required for InstructLab to generate synthetic data?

- 1
- **5** ✓
- 10
- 20

## Question 24
What technique does InstructLab use for parameter-efficient fine-tuning?

- LoRA
- **QLoRA (Quantized Low-Rank Adaptation)** ✓
- Adapter
- Prefix tuning

## Question 25
After fine-tuning a model with InstructLab, what is the command to convert it to GGUF format?

- ilab model serve
- ilab model test
- **ilab model convert** ✓
- ilab data generate

---

# Answers (InstructLab)

21. **Fine-tuning LLMs locally using synthetic data generation** - InstructLab uses teacher-student paradigm for synthetic data generation.

22. **Generates synthetic question-answer pairs from seed examples** - Teacher model (Merlinite) generates synthetic data from seed Q&A pairs.

23. **5** - Minimum 5 seed examples required; more examples produce better synthetic data.

24. **QLoRA (Quantized Low-Rank Adaptation)** - InstructLab uses QLoRA for parameter-efficient fine-tuning.

25. **ilab model convert** - Converts fine-tuned model to GGUF format for serving.

---

## Additional DPO Questions

## Question 26
What is the primary role of a partition function?

- **It normalizes probabilities in custom probability functions.** ✓
- It helps obtain a policy and its parameters that maximize the RL objective function.
- It helps collect data on human preferences.
- It forms the basis for the logistic probability function.

---

## Question 27
If π* is the desired policy and π_ref an arbitrary policy, both representing the probability of Y given X, what is the condition to minimize the KL-divergence to zero?

- π*(Y|X) > π_ref(Y|X)
- **π*(Y|X) = π_ref(Y|X)** ✓
- π*(Y|X) ≥ 0
- π*(Y|X) < π_ref(Y|X)

---

## Question 28
Which of the following equations represents the optimal solution for the DPO objective?

- L_R(φ,D) = -∑_n=1^N ln(σ(r(X_n,Y_n,w|φ) - r(X_n,Y_l|φ))
- Z(X) = ∑_y π_ref(Y|X) exp(1/β r(X,Y))
- **π_r(Y|X) = [π_ref(Y|X) · exp(1/β · r(X,Y))] / Z(X)** ✓
- -σ(β ln(π_r(Y_w|X)/π_ref(Y_l|X)) - β ln(π_r(Y_l|X)/π_ref(Y_l|X)))

---

## Question 29
Which of the following is correct about the training argument beta parameter?

- It is applied to the attention parameters to speed up model training.
- It has a fixed value of 0.1.
- **It is a temperature parameter for DPO loss.** ✓
- It applies the process function to the whole data set.

---

## Question 30
What are the steps to fine-tune a language model with DPO and Hugging Face?

- Step 1: Data preprocessing → Step 2: Define training arguments and DPO trainer → Step 3: Create and configure the model and tokenizer → Step 4: Plot the model's training loss → Step 5: Generate the model → Step 6: Inferencing
- **Step 1: Data preprocessing → Step 2: Create and configure the model and tokenizer → Step 3: Define training arguments and DPO trainer → Step 4: Plot the model's training loss → Step 5: Load the model → Step 6: Inferencing** ✓
- Step 1: Data preprocessing → Step 2: Create and configure the model and tokenizer → Step 3: Define training arguments and DPO trainer → Step 4: Load the model → Step 5: Inferencing → Step 6: Plot the model's training loss

---

# Answers (Additional DPO)

26. **It normalizes probabilities in custom probability functions.** - Partition function ensures probabilities sum to 1.

27. **π*(Y|X) = π_ref(Y|X)** - KL divergence is zero only when the two distributions are identical.

28. **π_r(Y|X) = [π_ref(Y|X) · exp(1/β · r(X,Y))] / Z(X)** - This is the optimal policy formula that defines the reward policy.

29. **It is a temperature parameter for DPO loss.** - Beta typically ranges from 0.1-0.5 in DPO training.

30. **Step 1: Data preprocessing → Step 2: Create and configure the model and tokenizer → Step 3: Define training arguments and DPO trainer → Step 4: Plot the model's training loss → Step 5: Load the model → Step 6: Inferencing** - Correct sequence for DPO fine-tuning with Hugging Face.

---

## Final Assessment Questions

## Question 31
Which of the following parameters controls the randomness of the distribution for the large language models (LLMs)?

- **Temperature** ✓
- Top K sampling
- Min max tokens
- Repetition penalty

---

## Question 32
What does rollout refer to in reinforcement learning for large language models (LLMs)?

- The policy uses the rewards collected during training for each reinforcement learning.
- The policy is initiated before training each query.
- **The model generates various responses for each query.** ✓
- The policy evaluates the performance of interactions with other departments.

---

## Question 33
Which of the following is the most suitable for ensuring the model learns to generate responses that align with human preferences while fine-tuning a pretrained large language model (LLM) using reinforcement learning with human feedback (RLHF)?

- **Fine-tune the pretrained LLM on a reward model.** ✓
- Use supervised learning to train the LLM on human feedback.
- Allow the LLM to generate responses without human feedback.
- Train the LLM from scratch using reinforcement learning signals.

---

## Question 34
You are designing a reinforcement learning algorithm and considering how to prevent drastic changes in policy updates. How does the KL penalty coefficient in PPO help to complete this task?

- Optimize temperature during sampling.
- Improve the model's gradient estimation.
- Increase variations in agents' responses.
- **Handle gaps between policies.** ✓

---

## Question 35
You want to simulate realistic training conditions by varying input text length in proximal policy optimization (PPO). Which utility of PPO will help you in this task?

- tokenizer.pad_token
- input_min_text_length
- input_size
- **LengthSampler** ✓

---

## Question 36
What is the primary role of PPOConfig class in Hugging Face's PPO training?

- Control decoding temperature
- **Define model and learning rate** ✓
- Execute token alignment
- Collect rollout logs

---

## Question 37
You are visualizing a scaled probability distribution of model outputs after applying a Gaussian function in direct preference optimization (DPO). How does the partition function help in this scenario?

- **Ensures probabilities from a valid distribution.** ✓
- Eliminates unaligned outputs from the model
- Amplifies differences between the reference and target models.
- Compares responses using reward-based scoring.

---

## Question 38
A researcher compares output probabilities under direct preference optimization DPO for different sequences and wants to ensure correct scaling behavior. Which option describes the outcome of applying the optimal DPO solution?

- **A policy that adjusts the reference model to the reward function using the beta parameter.** ✓
- A policy that focuses on the reference model and not reward scoring.
- A policy that clones the reward model completely.
- A policy that removes the normalization term from the computation in the reward function.

---

## Question 39
While evaluating two optimization techniques for fine-tuning LLMs and wanting to avoid unstable gradients, why would you use DPO instead of PPO to optimize a language model?

- **Revise the optimization problems to ignore numerical instability** ✓
- Restrict the human feedback to reduce training time
- Use settled vocabulary lookups to shorten tokenization
- Enhance the chance in sampling during inference

---

## Question 40
Which of the following models is suitable for causal text generation for a function call that loads a pretrained decoder?

- tokenizer.pad_token = tokenizer.eos_token
- tokenizer = GPT2Tokenizer.from_pretrained("gpt2")
- **model = AutoModelForCausalLM.from_pretrained("gpt2")** ✓
- model_ref = AutoModelForCausalLM.from_pretrained("gpt2")

---

# Answers (Final Assessment)

31. **Temperature** - Controls randomness/entropy of probability distribution in LLMs.

32. **The model generates various responses for each query** - Rollout is how the model generates different responses for each query.

33. **Fine-tune the pretrained LLM on a reward model** - RLHF uses reward models to align LLM outputs with human preferences.

34. **Handle gaps between policies** - KL penalty regulates divergence between old and new policies.

35. **LengthSampler** - Varies text lengths for data processing, enhancing model robustness.

36. **Define model and learning rate** - PPOConfig specifies model name and learning rate.

37. **Ensures probabilities from a valid distribution** - Partition function normalizes probabilities to sum to 1.

38. **A policy that adjusts the reference model to the reward function using the beta parameter** - Optimal solution scales reference model to reward function.

39. **Revise the optimization problems to ignore numerical instability** - DPO reformulates RL problem to avoid complex reward modeling and numerical issues.

40. **model = AutoModelForCausalLM.from_pretrained("gpt2")** - AutoModelForCausalLM is for causal (decoder) text generation.